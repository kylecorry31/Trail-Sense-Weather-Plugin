name: Nightly
permissions:
  contents: read

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: 17

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build APK
        run: ./gradlew assembleNightly

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: Trail-Sense-Weather-Plugin-Nightly.apk
          path: ./app/build/outputs/apk/nightly/*.apk
          retention-days: 7
